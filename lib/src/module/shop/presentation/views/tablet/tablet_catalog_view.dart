import 'package:flutter/material.dart';

class TabletCatalogView extends StatelessWidget {
  const TabletCatalogView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
